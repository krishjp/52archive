{
  "name": "52archive",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "node node_modules/next/dist/bin/next dev apps/web",
    "dev:server": "node --import tsx --watch apps/server/src/index.ts",
    "dev:clean": "node -e \"const fs = require('fs'); fs.rmSync('apps/web/.next', { recursive: true, force: true });\" && node node_modules/next/dist/bin/next dev apps/web",
    "dev:ios": "npm run start --workspace=mobile",
    "check": "npm run check --workspaces",
    "lint": "npm run lint --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "db:push-yaml": "npm run db:push-yaml --workspace=packages/core",
    "db:clear": "npm run db:clear --workspace=packages/core"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "react": "19.2.6",
    "react-dom": "19.2.6"
  },
  "overrides": {
    "react-native": {
      "react": "18.3.1"
    },
    "expo": {
      "react": "18.3.1"
    }
  }
}
